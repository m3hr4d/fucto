**یک توضیح خیلی ساده و خوش‌خوان از پروژهٔ fucto به زبان فارسی 🇮🇷**

---

## 🌟 خلاصهٔ خیلی ساده  
این پروژه یک **سرور FastAPI** است که **خودش را شبیه API رسمی OpenAI** رفتار می‌دهد. یعنی می‌توانی آن را روی سیستم خودت اجرا کنی و مثل ChatGPT با آن حرف بزنی.  
همچنین یک **نمونهٔ WebSocket** دارد که پیام‌ها را **زنده و لحظه‌ای** دریافت می‌کنی.

---

# 📘 توضیح کامل و ساده

## 🔧 این پروژه دقیقاً چیه؟
پروژهٔ **fucto** یک سرور ساده است که:
- مثل **OpenAI API** کار می‌کند  
- از **چندین کوکی** برای درخواست‌ها استفاده می‌کند (Load Balancing ساده)  
- با **FastAPI** نوشته شده  
- یک **اسکریپت WebSocket** دارد برای تست چت زنده  

---

# 📁 ساختار پروژه

| فایل | توضیح ساده |
|------|------------|
| `openai_api_server.py` | سرور اصلی FastAPI که endpoint شبیه `/v1/chat/completions` می‌سازد |
| `websocket_example.py` | نمونهٔ چت زنده با WebSocket |
| `cookies.txt` | لیست کوکی‌ها (برای استفادهٔ چرخشی) |
| `chat_id.txt` | ذخیرهٔ ID چت برای ادامهٔ گفتگو |
| `requirements.txt` | لیست پکیج‌های لازم |

---

# 🚀 چطور اجرا می‌شود؟

## 1) نصب پیش‌نیازها
```bash
pip install -r requirements.txt
```

## 2) ساخت فایل کوکی‌ها
یک فایل به نام `cookies.txt` بساز و کوکی‌ها را خط‌به‌خط داخلش بنویس.

مثال:
```
__client=xxxx
__client=yyyy
```

## 3) اجرای سرور API
```bash
python openai_api_server.py
```

بعد از اجرا، این مسیرها فعال می‌شوند:
- `/v1/chat/completions`
- `/v1/models`

این یعنی می‌توانی آن را مثل OpenAI API استفاده کنی.

## 4) اجرای WebSocket
```bash
python websocket_example.py
```

این اسکریپت:
- یک `chat_id.txt` می‌سازد  
- پیام می‌فرستی  
- پاسخ را لحظه‌ای دریافت می‌کنی  

---

# ❓ مشکلات رایج

### ❌ کوکی کار نمی‌کند (401 یا 403)
کوکی‌ها را دوباره از سایت بگیر و در `cookies.txt` جایگزین کن.

### ❌ پکیج‌ها نصب نمی‌شوند
مطمئن شو Python 3.10+ داری و پکیج‌های زیر نصب هستند:
- fastapi  
- websockets  

---

# 🎯 جمع‌بندی خیلی کوتاه
این پروژه یک **سرور شبیه OpenAI** است که:
- با FastAPI ساخته شده  
- از چند کوکی برای درخواست‌ها استفاده می‌کند  
- WebSocket زنده دارد  
- برای تست و توسعهٔ مدل‌ها یا پروکسی OpenAI عالی است  

---

اگر خواستی، می‌توانم:
- یک **آموزش قدم‌به‌قدم** برای راه‌اندازی بدهم  
- کد را **ساده‌سازی** کنم  
- یک **نسخهٔ فارسی‌شده** از README برایت بسازم  

دوست داری کدام را انجام بدهم؟ 😊
